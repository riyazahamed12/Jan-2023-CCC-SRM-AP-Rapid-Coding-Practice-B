#include<stdio.h>
#include<stdlib.h>
#include<string.h>
int main()
{
    char str[100];
    scanf("%s",str);
    int n =strlen(str);
    char temp = str[0];
    for(int i=0;i<n;i++){
        str[i]=str[i+1];
    }
    str[n-1] = temp;
    printf("%s",str);
    
	//your code here
	return 0;
}

